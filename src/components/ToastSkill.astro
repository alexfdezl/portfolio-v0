---
export interface Props {
    skill: string;
}
const {skill} = Astro.props;
---

<span class="p-2 rounded-full bg-gray-200 dark:bg-gray-600 text-sm hover:bg-gray-300 dark:hover:bg-gray-700 cursor-default">{skill}</span>